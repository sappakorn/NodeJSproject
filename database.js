/* const { Pool } = require('pg');
const dbconnect = new Pool({
     user: 'postgres',
     host:'localhost',
     database:'myDB',
     password: '1234',
     port: 5432
});

module.exports = dbconnect;

 */